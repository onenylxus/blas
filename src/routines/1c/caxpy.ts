// Import
import { Integer, I } from '../../types/integer';
import { SingleComplex, C } from '../../types/singleComplex';

// CAXPY routine
const caxpy = (n: number, ca: Dual, cx: Array<Dual>, incx: number, cy: Array<Dual>, incy: number): void => {

};

// Export
export default caxpy;
