// Import
import { Single, S } from '../../types/single';
import { SingleComplex, C } from '../../types/singleComplex';

// CROTG routine
const crotg = (a: Dual, b: Dual, c?: number, s?: Dual): void => {

};

// Export
export default crotg;
