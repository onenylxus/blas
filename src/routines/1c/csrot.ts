// Import
import { Integer, I } from '../../types/integer';
import { Single, S } from '../../types/single';
import { SingleComplex, C } from '../../types/singleComplex';

// CSROT routine
const csrot = (n: number, cx: Dual[], incx: number, cy: Dual[], incy: number, c: number, s: number): void => {

};

// Export
export default csrot;
