// Import
import { Dual } from '../../types/complex';
import { Integer, I } from '../../types/integer';
import { SingleComplex, C } from '../../types/singleComplex';

// CSWAP routine
const cswap = (n: number, cx: Array<Dual>, incx: number, cy: Array<Dual>, incy: number): void => {

};

// Export
export default cswap;
