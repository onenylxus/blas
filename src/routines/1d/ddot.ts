// Import
import { Double, D } from '../../types/double';
import { Integer, I } from '../../types/integer';

// DDOT routine
const ddot = (n: number, dx: Array<number>, incx: number, dy: Array<number>, incy: number) => {

};

// Export
export default ddot;
