// Import
import { Double, D } from '../../types/double';
import { Integer, I } from '../../types/integer';

// DROT routine
const drot = (n: number, dx: Array<number>, incx: number, dy: Array<number>, incy: number, c: number, s: number): void => {

};

// Export
export default drot;
