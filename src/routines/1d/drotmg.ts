// Import
import { Double, D } from '../../types/double';

// DROTMG routine
const drotmg = (dd1: number, dd2: number, dx1: number, dy1: number, dparam: number[]): void => {

};

// Export
export default drotmg;
