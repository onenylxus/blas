// Import
import { Double, D } from '../../types/double';
import { Integer, I } from '../../types/integer';

// DSWAP routine
const dswap = (n: number, dx: Array<number>, incx: number, dy: Array<number>, incy: number): void => {

};

// Export
export default dswap;
