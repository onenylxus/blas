// Import
import { Double, D } from '../../types/double';
import { Integer, I } from '../../types/integer';

// DSWAP routine
const dswap = (n: number, dx: number[], incx: number, dy: number[], incy: number): void => {

};

// Export
export default dswap;
