// Import
import { Double, D } from '../../types/double';
import { Integer, I } from '../../types/integer';

// IDAMAX routine
const idamax = (n: number, dx: Array<number>, incx: number): void => {

};

// Export
export default idamax;
