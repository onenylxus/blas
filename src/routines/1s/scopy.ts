// Import
import { Integer, I } from '../../types/integer';
import { Single, S } from '../../types/single';

// SCOPY routine
const scopy = (n: number, sx: number[], incx: number, sy: number[], incy: number): void => {

};

// Export
export default scopy;
