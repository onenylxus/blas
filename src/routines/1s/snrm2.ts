// Import
import { Integer, I } from '../../types/integer';
import { Single, S } from '../../types/single';

// SNRM2 routine
const snrm2 = (n: number, x: Array<number>, incx: number): void => {

};

// Export
export default snrm2;
