// Import
import { Integer, I } from '../../types/integer';
import { Single, S } from '../../types/single';

// SROTM routine
const srotm = (n: number, sx: number[], incx: number, sy: number[], incy: number, sparam: number[]): void => {

};

// Export
export default srotm;
