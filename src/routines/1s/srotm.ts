// Import
import { Integer, I } from '../../types/integer';
import { Single, S } from '../../types/single';

// SROTM routine
const srotm = (n: number, sx: Array<number>, incx: number, sy: Array<number>, incy: number, sparam: Array<number>): void => {

};

// Export
export default srotm;
