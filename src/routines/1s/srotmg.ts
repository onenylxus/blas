// Import
import { Single, S } from '../../types/single';

// SROTMG routine
const srotmg = (sd1: number, sd2: number, sx1: number, sy1: number, sparam?: Array<number>): void => {

};

// Export
export default srotmg;
