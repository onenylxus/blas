// Import
import { DoubleComplex, Z } from '../../types/doubleComplex';
import { Integer, I } from '../../types/integer';

// ZAXPY routine
const zaxpy = (n: number, za: Dual, zx: Array<Dual>, incx: number, zy: Array<Dual>, incy: number): void => {

};

// Export
export default zaxpy;
