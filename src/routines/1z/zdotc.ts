// Import
import { DoubleComplex, Z } from '../../types/doubleComplex';
import { Integer, I } from '../../types/integer';

// ZDOTC routine
const zdotc = (n: number, zx: Array<Dual>, incx: number, zy: Array<Dual>, incy: number): void => {

};

// Export
export default zdotc;
