// Import
import { DoubleComplex, Z } from '../../types/doubleComplex';
import { Integer, I } from '../../types/integer';

// ZDOTU routine
const zdotu = (n: number, zx: Dual[], incx: number, zy: Dual[], incy: number): void => {

};

// Export
export default zdotu;
