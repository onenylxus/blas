// Import
import { Dual } from '../../types/complex';
import { DoubleComplex, Z } from '../../types/doubleComplex';
import { Integer, I } from '../../types/integer';

// ZDOTU routine
const zdotu = (n: number, zx: Array<Dual>, incx: number, zy: Array<Dual>, incy: number): void => {

};

// Export
export default zdotu;
