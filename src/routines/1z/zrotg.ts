// Import
import { Double, D } from '../../types/double';
import { DoubleComplex, Z } from '../../types/doubleComplex';

// ZROTG routine
const zrotg = (a: Dual, b: Dual, c: number, s: Dual): void => {

};

// Export
export default zrotg;
